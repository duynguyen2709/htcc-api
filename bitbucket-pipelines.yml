image: maven:3-jdk-8

pipelines:
  default:
    - parallel:
      - step:
         caches:
           - maven
         script: 
           - cd htcc-gateway-service/
           - mvn -B package
      - step:
         caches:
           - maven
         script: 
           - cd htcc-employee-service/
           - mvn -B package
